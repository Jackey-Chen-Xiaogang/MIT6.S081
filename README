1.先在thread的结构体里面定义一个寄存器的结构体，这里只需要定义s和ra和sp，ra是返回地址寄存器，sp当前线程的栈，s保存寄存器的状态，
使用 t->context.sp = (uint64)t->stack + STACK_SIZE;将栈指针初始化到栈顶部（栈从高往低生长），然后将返回地址设置为传入的函
数确保第一次执行该函数，然后用汇编获得寄存器a0和a1（swtch_thread函数传入的参数）并且存储旧的加载新的。
2.哈希多线程会出现哈希键丢失的原因在于插入的时候出现竞争：两个进程同时看上了哈希中的一个桶，所以要在加入元素的时候给对应的桶加锁。
3.只需注意如果多个线程同时操作bstat会导致混乱，所以要使用锁每次只允许一个线程操作。
