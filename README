lab report

1.看不懂汇编，跑路
2.要把调用函数的返回地址打印出来，先用两个宏把当前栈页表的上下限找到（其实只用找最低就行，栈里面的地址从大到小递减），然后先偏移8格找到储存返回地址的地址，然后解指针（不是直接返回，我就说怎么可能返回地址之间的距离是固定的，每个函数占用大小都不一样）， 然后更新一下帧指针继续找，一直到找到页表底部。
3.alarm
  (1)invoke handler:进程里面开三个位置，一个存过去的时间，一个存要走的时间，一个存传入的需要执行的函数的地址，当调用sigalarm函数的时候，把该地址赋值给程序计数器，然后中断结束之后就可以执行该函数
  (2)在进程里面开一个寄存器的备份（记得初始化记得释放），在开一个是否在处理alarm的标志位（如果不开，而且时间片很短就会在上一次还没处理完下一次就开始处理，会复写进程的内存），最后在sigreturn里面恢复现场（p->trampline是指针，要解引用，服了）
