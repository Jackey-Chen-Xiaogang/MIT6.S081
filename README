1.需要实现每个cpu拥有自己的空闲列表，当分配内存的时候从自己的空闲列表中获得，而当前CPU没有空闲列表的时候就从其他CPU偷走内存，
一开始为实现的是每次没有内存的时候就轮询一次找一个有空闲的分一个过去，后面看别人的实现，可以用快慢指针一次将其他CPU的一半给偷
走，在某个CPU负荷远远大于其他CPU的时候感觉很有用。
2.需要重新实现bcache从而减少锁的竞争，提高性能。基本思路是利用哈希，将磁盘中的inode映射到内存中，每次访问的时候通过哈希找到
对应的缓存，要是没有找到就从磁盘中读取。这里的实现结合了哈希表和链表：首先以 结点数%桶数 作为头结点，之后将对应磁盘中的结点加
载到链表中。查询的时候；利用哈希值找到头结点，然后遍历该头结点对应的链表找到对应的缓存。每一个哈希桶都要使用一个锁来保存。当某
个桶的缓存位置不够了就从其他桶拿。