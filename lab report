1.sleep
感觉没啥难的，调用api就行了
2.pingpong
创建两个进程然后互相发信息，C里面可以用pipe函数给让有两个元素的数组有两个文件描述符，然后[0]是读端，[1]是写端，通常一个进程要只能用一个端单向通信，双向要两个管道
3.primes
一开始没看懂题目，后面搜到sieve是筛法，然后就知道要一开始把全部数字给第一个进程，然后第一个进程筛掉当前的第一个元素的倍数，因为当前第一个元素肯定是素数，相当于每一
个进程都是一个筛子，筛掉当前素数的所有倍数，并且输出。

实现方式感觉也挺tricky，要用递归实现，而且很神奇的是pipe(fd[2])之后fd[0]和fd[1]可以作为一个int 类型的参数传递给函数，然后在函数里面读取。
